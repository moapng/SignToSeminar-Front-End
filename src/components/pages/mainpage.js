import React, { Component } from 'react';

import Menu from "../seminars/seminar-menu";

export default class MainPage extends Component {
    render() {
        return (
            <div>
                <Menu />
            </div>
        );
    }
}